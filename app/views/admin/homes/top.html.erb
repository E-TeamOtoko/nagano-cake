<div class="container">
  <div class="row">
    <div class="col-sm-12 col-md-8 px-5 px-sm-0 mx-auto">
      <h1>商品一覧</h1>
        <table>
          <thead>
            <tr>

                <td>商品ID</td>


                <td>商品名</td>


                <td>税抜価格</td>


                <td>ジャンル</t>


                <td>ステータス</td>

            </tr>
          </thead>
          <tbody>
            <% @items.each do |item| %>
              <tr>

                <td><%= item.id %></td>
                <td>
                  <a href="<%= item_path(item.id) %>"><%= item.item_name %></a>
                </td>
                <td><%= item.tax_excluded_price %></td>
                <td><%= item.genre.name %></td>
                <td>
                  <% if item.sales_status === false %>
                    販売停止中
                  <% else %>
                    販売中
                  <% end %>
                </td>

                </tr>
            <% end %>
          </tbody>
        </table>
    </div>
  </div>
</div>